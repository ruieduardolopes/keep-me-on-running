package hippodrome.responses;

import entities.Spectator;
import entities.SpectatorState;

public class Response {
    public Response(ResponseType state, int value, SpectatorState newState) {
        if (state == SpectatorState.PLACING_A_BET)
    }
}
